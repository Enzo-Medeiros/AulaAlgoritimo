#include <stdio.h>
#include <stdlib.h>
#include <time.h>
int main(){
srand(time(NULL));
int numR=rand() % 10+1;
int op;
printf("Tente adivinhar o numero:");
        do{
            
            scanf("%d", &op);
            if(op!=numR){
                printf("Voce errou\n");
            }else
            printf("Voce acertou\n");
        }while(op!=numR);
}
